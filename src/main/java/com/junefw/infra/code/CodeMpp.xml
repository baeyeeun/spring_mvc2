<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.junefw.infra.modules.code.codeMpp">

    <resultMap id="resultMapObj" type="com.junefw.infra.modules.code.Code"></resultMap>

	<select id="selectList" resultMap="resultMapObj">
			SELECT
				a.ifcdSeq
				, a.ifcdName
				, a.ifcdDelNy
			FROM
				ifraCode a
			WHERE 1=1
			<choose>
				<when test="shIfchDelNy == 1"> AND ifcgDelNy = 1</when>
				<when test="shIfchDelNy == 0"> AND ifcgDelNy = 0</when>
			</choose>
				AND a.ifcdDelNy=0 
				<if test="shIfcgName != and !shIfcgName.equals('')">AND ifcgName like concat('%', #{shIfcgName}, '%')</if>
				
	</select>
		
	<insert id="insert">
		INSERT INTO infaCode(
			ifcdName
			, ifcdDelNy
		) values (
			 #{ifcdName}
			, 0
		)
	</insert>

	<select id="selectOne" resultMap="resultMapObj">
			SELECT
				a.ifcdSeq
				, a.ifcdName
				, a.ifcdDelNy
			FROM
				infrCode a
			WHERE 1=1
				AND ifcdSeq = #{ifcdSeq}
	</select>
	
	<update id="update">
		UPDATE infrCode SET
			ifcdName = #{ifcdName}
		WHERE 1=1
			AND ifcdSeq = #{ifcdSeq}
	</update>
	

	<!-- infr -->
	<select id="selectListCode" resultMap="resultMapObj">
			select
				a.ifcgSeq
				, a.ifcgName
				, a.ifcgDelNy
			FROM
				infrCodeGroup a
			WHERE 1=1
				AND ifcgDelNy=0 
				<if test="shIfcgSeq != null and !shIfcgSeq.equals('')">AND IfcgSeq = #{shIfcgSeq}</if>
	</select>
		
	<insert id="insertCode">
		INSERT INTO infrCodeGroup(
			ifcgName
			, ifcgDelNy
		) values (
			 #{ifcgName}
			, 0
		)
	</insert>

	<select id="selectOneCode" resultMap="resultMapObj">
			select
				a.ifcgSeq
				, a.ifcgName
				, a.ifcgDelNy
			FROM
				infrCodeGroup a
			WHERE 1=1
				AND ifcgSeq = #{ifcgSeq}
	</select>
	
	<update id="updateCode">
		UPDATE infrCodeGroup SET
			ifcgName = #{ifcgName}
		WHERE 1=1
			AND ifcgSeq = #{ifcgSeq}
	</update>
	
</mapper>